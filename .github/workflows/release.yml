name: "Release"

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Build and package
        run: npm run prepare
      - name: Verify build
        run: |
          if [ ! -f ./dist/sourcemap-register.js ]; then
            echo "Error: sourcemap-register.js is missing!"
            exit 1
          fi
          node -e "try { require('./dist/index.js'); console.log('Successfully loaded index.js'); } catch (e) { console.error(e); process.exit(1); }"
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/index.js
            dist/index.js.map
            dist/sourcemap-register.js
            dist/licenses.txt
            action.yml
          body: |
            Release ${{ github.ref_name }}

            Automatically created by the release workflow.
